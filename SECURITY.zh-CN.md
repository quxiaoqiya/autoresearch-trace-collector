# 安全策略

[English](SECURITY.md) | [简体中文](SECURITY.zh-CN.md)

## 支持版本

本项目尚未正式发布。安全修复目前面向默认分支，尚未承诺维护任何已发布版本。

## 报告安全问题

如果仓库启用了 GitHub 私密漏洞报告，请优先使用该功能。不要在公开 Issue 中发布凭据、私密 Trace、未脱敏 prompt 或漏洞利用细节。如果无法私密报告，请创建一个不包含敏感信息的公开 Issue，请维护者提供私密联系渠道。

报告应包含受影响版本、平台、只使用合成数据的最小复现、预期行为和影响。附加文件之前必须移除所有真实秘密。

## Trace 数据处理

- 默认将 `trace.jsonl`、`events.jsonl`、`stderr.log` 和 `manifest.json` 视为敏感数据。
- 脱敏属于尽力而为，可能遗漏未知编码、未知格式或普通文本中的秘密。
- 不要采集身份认证文件、环境变量转储、Cookie、私钥、原始凭据、私有数据集或完整 prompt。
- 如果任何产物包含凭据，应首先撤销或轮换凭据；仅删除文件并不足够。
- 共享产物或将其附加到 Issue 前应逐一人工检查。

## 安全范围

值得报告的问题包括：绕过凭据脱敏、不安全路径处理、文件完整性错误、锁或追加损坏、命令注入路径，以及错误接受畸形输入。

本采集器不承诺提供保密边界、防篡改存储、沙箱隔离或科学结果验证。
